import { print, clear, askQuestion } from "./consoleBuilder";

export function systemStart(): void {
    clear(false);
    print("--------------------------");
    print("|     System Start !     |");
    print("--------------------------");

    // askQuestion(`What's your name? `, startAdventure);
}

// function
